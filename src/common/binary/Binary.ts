enum Binary {
    UInt8 = 1,
    Int8,
    UInt16,
    Int16,
    UInt32,
    Int32,
    Float32,
    Float64,
    Boolean,
    String,
    UInt8Array,
    Int8Array,
    UInt16Array,
    Int16Array,
    UInt32Array,
    Int32Array,
    Float32Array,
    Float64Array,
}

export { Binary }
